<Page
    x:Class="StoreApp01.WebBrow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StoreApp01"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
       
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
            
            <SearchBox Height="50" Grid.Column="0" Margin="0,10,0,0" QuerySubmitted="SearchBox_QuerySubmitted"/>
        <AppBarButton Icon="Send" Grid.Column="1"  VerticalAlignment="Top" HorizontalAlignment="Right" Click="AppBarButton_Click"/>
            </Grid>
       


        <WebView Grid.Row="1" x:Name="webHtml"/>

    </Grid>
</Page>
